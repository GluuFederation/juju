name: gluu-server
summary: gluu-server for OpenID Connect OP and UMA AS
maintainer: shouro <shouro@gluu.org>
description: |
  This charm deploy gluu-server (oxauth and oxtrust) with tomcat7.
categories: ['app-servers']
provides:
  oxserver:
    interface: openid-connect-op
requires:
  ldapserver:
    interface: gluuldap
